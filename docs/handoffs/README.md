# Handoff Archive

This directory stores historical session handoffs, audits, and closeout notes that were previously loose in the repo root.

They are retained for context, but they are not load-bearing runtime code. Prefer current tracked docs such as `AGENTS.md` and `README.md` before digging through archived handoffs.

